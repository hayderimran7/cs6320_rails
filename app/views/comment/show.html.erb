<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>PhotoSite, from Imran Malik</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="CS-6320 Project 1">
  <meta name="author" content="Imran Malik">
</head>
<body>
<div class="container">
  <div class="jumbotron text-center col-mid-6">
    <h1 class="text-primary">Photosite Comments Page </h1>
  </div>
</div>

<div class="container">
  <h1 class="bg-primary">Comments for photo <%= @photo_id %>  of User  <strong> <%= @user.first_name  %> <%= @user.last_name %> </strong></h1>

  <% if @has_comment==true %>
      <ul class="list-group">
      <% @comments.each do |comment| %>
        <li class="list-group-item list-group-item-success"><strong>Comment: </strong><%= comment.comment_text %> <br> <strong>Created at: </strong><%= comment.date_time %></li>
      <% end %>
      </ul>
  <% else %>
      <p class="alert alert-danger"><strong>WARNING:</strong> No comments found</p>
  <% end %>
</div>
</body>
</html>